<h2><%= @message.title%></h2>
<p><%= @message.description%></p>

<%=link_to "Edit", edit_message_path(@message)%>
<%=link_to "Home", root_path%>
<%=link_to "Delete", message_path(@message), method: :delete, data: { confirm: "Are you sure you wan to permanently delete this note" }%>

